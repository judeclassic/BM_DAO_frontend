'use client'

/* Core */
import { Provider } from 'react-redux'

/* Instruments */

export const Providers = (props: React.PropsWithChildren) => {
  return (
        <>
            {props.children}
        </>
      )
}
