import React from "react";
import Raiders from "../_components/raiders";

const Page = () => {
    return (
        <>
            <Raiders/>
        </>
    );
};

export const metadata = {
    title: "BM DAO | Twitter Raiders",
};

export default Page;
